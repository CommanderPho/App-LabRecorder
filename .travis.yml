## Travis CI configuration removed
##
## macOS builds for this project were previously run on Travis CI, but
## those macOS targets are no longer supported, so the CI job has been
## intentionally disabled.